const passengers = ['Athul', 'Ramesh', 'Bibin', 'Deepak', 'Hari', 'Ramu', 'Rajesh', 'Sojan', 'Ashwin', 'Karnan'];
const search = 'Ramu';

for (let index = 0; index < passengers.length; index++) {
    if(passengers[index] === 'Ramu')
    {
        console.log("find keyword in "+ index);
    }
}